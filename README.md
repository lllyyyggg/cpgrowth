```
        /* 1.首先将字符串转化为标准的样式 */
        /* 2.然后根据类标签将不同的数据分散到不同的文件中 */
        /* 3.然后计算两个文件中的ITEMCOUNT并将COUNT存入到文件中 */
        /* 4.然后根据COUNT从两个文件中合并两个数据集到一个文件中 */
        /* 5.然后就可以开始建树、merge、挖掘模式等操作了 */
```